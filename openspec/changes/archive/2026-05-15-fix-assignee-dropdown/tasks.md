## 1. 인라인 카드 생성 — 담당자 드롭다운

- [x] 1.1 `openAddCard()` 함수에 담당자 `<select>` 요소 추가 (팀 멤버 목록 + 미할당)
- [x] 1.2 드롭다운 기본값을 현재 사용자(`user.id`)로 설정
- [x] 1.3 Enter 저장 시 선택된 `assignee_id` (미할당이면 null) 포함해서 API 호출
- [x] 1.4 인라인 입력 박스 닫힐 때 select 초기화

## 2. 카드 상세 모달 — 담당자 변경

- [x] 2.1 모달의 "담당자" 행을 텍스트에서 `<select>` 드롭다운으로 교체
- [x] 2.2 "다른 담당자 지정" 버튼 제거 (드롭다운으로 대체)
- [x] 2.3 모달 열릴 때 현재 `assignee_id`로 select 초기값 설정
- [x] 2.4 `saveModal()` 에서 select 값을 읽어 `PUT /tasks/{id}` 호출 시 포함

## 3. 카드 assignee 표시 개선

- [x] 3.1 `memberEmail()` 함수에서 이메일 축약(`split('@')[0]`) 제거 → 전체 이메일 표시
- [x] 3.2 현재 사용자는 `@me`로 유지, 타인은 이메일 전체 표시
